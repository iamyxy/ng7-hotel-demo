import { ColorRedAlertHelloDirective } from './color-red-alert-hello.directive';

describe('ColorRedAlertHelloDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRedAlertHelloDirective();
    expect(directive).toBeTruthy();
  });
});

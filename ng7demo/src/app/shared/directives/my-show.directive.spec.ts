import { MyShowDirective } from './my-show.directive';

describe('MyShowDirective', () => {
  it('should create an instance', () => {
    const directive = new MyShowDirective();
    expect(directive).toBeTruthy();
  });
});
